[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
markers =
    integration: tests requiring external services (DB, Redis)
filterwarnings =
    ignore::DeprecationWarning
